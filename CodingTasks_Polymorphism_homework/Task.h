// Implement a function printArea() that takes a pointer to the base class Shape as an argument and calls a method in both derived
// classes Circle and Rectangle. The area() area of the shape, assuming Circle has an attribute radius and Rectangle has width and height.
